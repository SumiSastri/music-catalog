import React from 'react';
import { Container, Button } from 'reactstrap';

const deleteButton = (properties) => {
	return (
		<Container>
			<Button color="danger" style={{ marginBotton: '2rem' }}>
				{' '}
			</Button>
		</Container>
	);
};

export default deleteButton;
