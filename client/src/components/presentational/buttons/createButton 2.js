import React from 'react';
import { Container, Button } from 'reactstrap';

const createButton = (properties) => {
	return (
		<Container>
			<Button color="primary" style={{ marginBotton: '2rem' }}>
				{' '}
			</Button>
		</Container>
	);
};

export default createButton;
