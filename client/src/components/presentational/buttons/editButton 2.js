import React from 'react';
import { Container, Button } from 'reactstrap';

const editButton = (properties) => {
	return (
		<Container>
			<Button color="grey" style={{ marginBotton: '2rem' }}>
				{' '}
			</Button>
		</Container>
	);
};

export default editButton;
