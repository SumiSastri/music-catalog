import React from 'react';
import { Container, Button } from 'reactstrap';

const submitButton = (properties) => {
	return (
		<Container>
			<Button color="success" style={{ marginBotton: '2rem' }}>
				{' '}
			</Button>
		</Container>
	);
};

export default submitButton;
