{
  "name": "server",
  "version": "1.0.0",
  "description": "- MongoDB has models/ collections and schemas - Models - represents the collections in the db (user model, product model, etc.) - Collection - a collection of data/ documents/ fields (represents a single model all the products in the product model) - Schemas - define the structure of the objects in the collection (the structure of a single product object)",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node server.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "body-parser": "^1.19.0",
    "concurrently": "^5.3.0",
    "cors": "^2.8.5",
    "dotenv": "^8.2.0",
    "express-rate-limit": "^5.2.3",
    "helmet": "^4.2.0",
    "mongodb": "^3.6.3",
    "mongoose": "^5.11.8",
    "nodemon": "^2.0.6",
    "path": "^0.12.7"
  }
}
